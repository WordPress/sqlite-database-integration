{
    "name": "wordpress/mysql-proxy",
    "description": "A MySQL proxy that bridges the MySQL wire protocol to a PDO-like interface.",
    "type": "library",
    "license": "GPL-2.0-or-later",
	"bin": [
		"bin/wp-mysql-proxy.php"
	],
	"scripts": {
		"test": "phpunit"
	},
    "require-dev": {
        "phpunit/phpunit": "^8.5",
        "symfony/process": "^5.4"
    },
	"autoload": {
		"classmap": [
			"src/"
		]
	}
}
